# Runtime dependencies for the full Qt FreeCleaner build.
# Build 35 uses the current Qt for Python line on a supported CPython runtime.
# Pillow is intentionally not installed: the app uses Qt image codecs and native .ico assets only.
# PySide6 6.11.1 requires Python >=3.10,<3.15 and publishes Windows x64/ARM64 wheels, not win32.
PySide6==6.11.1; python_version>='3.10' and python_version<'3.15'

# Optional build dependency.
pyinstaller>=6.0,<7.0; python_version>='3.10'
